# Контекст проекта

## О проекте

Дипломный проект: **интернет-магазин средней сложности на Python + Django** с
интеллектуальными модулями на основе языковых моделей.

Основа — рабочий прототип (бывшая курсовая). Задача диплома — развить его:
закрыть технический долг, расширить функционал и добавить AI-модули.

**Автор:** Шевырев Дмитрий Сергеевич, КубГУ, ФКТиПМ, 3 курс.
**Репозиторий:** https://github.com/DmitriyShevyrev/kursovoi_project

## Стек и окружение

- **ОС разработчика:** Windows. Все команды/пути — под Windows (PowerShell/CMD).
- **Python 3.13, Django 6.0.4**, архитектура MTV.
- **БД:** SQLite (разработка) → **PostgreSQL** (цель диплома).
- **Фронтенд:** Django-шаблоны + Bootstrap 5 (через CDN). Без React и JS-фреймворков.
- **AI:** `sentence-transformers` (локально, для поиска) + Gemini Flash free API
  (генеративные задачи). Всё через абстрактный слой-провайдер.

Виртуальное окружение лежит внутри проекта: запускать через `venv\Scripts\python.exe`.

## Структура (4 приложения)

- `catalog` — товары, категории, карточки, поиск, фильтрация. Модели: Category,
  Product, ProductImage.
- `cart` — корзина на сессиях + синхронизация в БД через signals. Модели: Cart, CartItem.
- `orders` — оформление, статусы, история, отмена заказов. Модели: Order, OrderItem.
- `users` — регистрация, авторизация, личный кабинет. Модель: Profile.
- `config` — настройки проекта, корневые urls.

Особенности: корзина двухуровневая (SessionCart + БД, синхронизация через signals);
цена фиксируется в OrderItem на момент покупки; Profile создаётся автоматически
через signals; поиск сейчас через `icontains`.

## Данные и воспроизводимость

`db.sqlite3` намеренно не хранится в репозитории. Каталог разворачивается из фикстуры:

```
copy .env.example .env
venv\Scripts\python.exe -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
:: полученный ключ вписать в .env в переменную SECRET_KEY

venv\Scripts\python.exe manage.py migrate
venv\Scripts\python.exe manage.py loaddata catalog.json
```

Без `.env` проект намеренно падает с ошибкой `SECRET_KEY not found`: запасного
значения в коде нет, чтобы секрет не мог снова утечь в репозиторий.

Фикстура `catalog/fixtures/catalog.json` содержит 5 категорий и 52 товара.
Картинки к ним лежат в `media/products/` и **не игнорируются** — без них
каталог после клонирования показывал бы пустые карточки.

При пересоздании фикстуры нельзя использовать флаг `-o`: Django открывает файл
в кодировке Windows (cp1251) и падает на символе `²`. Вместо этого:

```
set PYTHONIOENCODING=utf-8
venv\Scripts\python.exe manage.py dumpdata catalog --indent 2 > catalog\fixtures\catalog.json
```

## Правила работы (важно)

- Я студент, с Python знаком поверхностно, с Django — впервые. **Всегда объясняй
  «почему», а не только «как».** Новые концепции Django — через аналогии.
- **Простое рабочее решение важнее идеального архитектурного.** Цель — диплом, не
  production. Не переусложняй.
- Если предлагаешь два способа — выбирай более простой и поясняй отличие.
- Добавляй комментарии в код — они часть обучения.
- Работаем в ветке `diploma`, курсовую в `main` не трогаем.
- Спрашивай подтверждение перед необратимыми действиями.
- Если от меня что-то требуется сделать руками — давай чёткую пошаговую инструкцию.

## Приватность (проектное требование)

Персональные данные клиентов (реальные ФИО, адреса, телефоны) **не отправляются
во внешние LLM**. В облачные модули идут только данные каталога и обезличенный
текст (принцип минимизации ПДн, 152-ФЗ). Провайдер сменный: возможен переход на
локальную модель без изменения кода модулей.

## Состояние репозитория

В репозитории две **несвязанные** истории (общего предка нет):

- `main` — курсовая. Пять старых коммитов заливались через веб-интерфейс GitHub,
  поэтому в них попали `__pycache__` и `db.sqlite3`. Коммитом «Зафиксировать
  финальное состояние курсовой работы» это вычищено, а недостающие файлы добавлены.
- `claude/seed-products-database-gpCCh` — тупиковая ветка, **не мержить**. Её команда
  `seed_products` создаёт товары без картинок и заводит категории, которых нет в каталоге.
- `diploma` — рабочая ветка. Отведена от `main`.

`db.sqlite3` остаётся в пяти старых коммитах `main`. Переписывать историю ради
косметики не стали: персональных данных там нет.

## План работы по этапам

**Этап 1 — Технический долг.**
~~`.gitignore`~~, ~~`.env` + вынос секретов (python-decouple)~~, ~~`requirements.txt`~~,
`README.md`. Миграция SQLite → PostgreSQL. ~~Первые тесты~~ (было 12, стало 15).
Логирование.

Старый `SECRET_KEY` лежал открытым текстом в `config/settings.py` и попал в публичную
историю git с первых коммитов. Считается скомпрометированным: ключ заменён на новый,
он хранится только в `.env`. Чистить историю не стали — ключ отозван, смысла нет.

**Этап 2 — Функциональные доработки.**
Модель `Review` (отзывы + рейтинг). REST API (Django REST Framework).
Расширение админки. Опционально — заглушка оплаты.

**Этап 3 — Абстрактный слой AI + приватность.**
Приложение `ai/` с интерфейсом `AIProvider` (generate/embed/chat).
Провайдеры: `MockProvider` (разработка), `GeminiProvider` (Gemini Flash free,
retry на 429 + кэш), `OllamaProvider` (локальный, опция). Модуль обезличивания ПДн.

**Этап 4 — Умный поиск / рекомендации.**
`sentence-transformers` (multilingual-e5) локально. Индексация эмбеддингов товаров
(пересчёт через post_save). Семантический поиск по косинусной близости +
рекомендации «похожие товары».

**Этап 5 — Чат-бот-консультант.**
Виджет чата (JS + Bootstrap). Бэкенд через GeminiProvider. Function calling / RAG
по данным каталога (наличие, цена, характеристики). ПДн в промпт не передаются.

**Этап 6 — Автоответы на отзывы.**
Генерация ответа с разветвлением по рейтингу. Прогон через модуль обезличивания.
Режим «черновик + ручное одобрение» в админке.

**Этап 7 — Аналитика для админа.**
Суммаризация отзывов, выявление частых жалоб (на обезличенных данных). Сводка по
продажам на естественном языке. Дашборд в админке.

**Этап 8 — Тестирование и метрики.**
Метрики релевантности поиска, оценка качества ответов, проверка обезличивания
(ПДн не утекают), замеры производительности.

**Этап 9 — Пояснительная записка (параллельно).**
Введение → аналитическая → проектная → практическая → заключение → приложения.
Отдельный подраздел «Обработка персональных данных и приватность».

## Технический долг (найденное попутно)

- `cart/__init__.py` содержит `default_app_config` — конструкция удалена из Django 4.1,
  сейчас просто игнорируется. Убрать.
- `cart/apps.py` и `cart/__init__.py` — без перевода строки в конце файла.
- Поиск в каталоге через `icontains` — будет заменён семантическим на Этапе 4.
- У двух товаров категории «Строительство и ремонт» («Обойный шпатель» и набор
  резиновых шпателей) вместо фотографий загружены слайды презентации — файлы
  `Слайд_1_zqmeicb.PNG` и `1_U7b08MO.1_слайд.PNG`, вместе 3.5 МБ. Заменить на
  нормальные изображения и пересоздать фикстуру.

## Текущий статус

**Этап 1, в процессе.** Сделано: гигиена репозитория (git приведён в порядок,
`.gitignore`, фикстура каталога, ветка `diploma`). Дальше: `.env` и вынос
секретов, `README.md`, логирование, миграция на PostgreSQL.
