# Образец файла окружения.
#
# Скопируйте его в .env и подставьте свои значения:
#     copy .env.example .env
#
# Сам .env в репозиторий не попадает — в нём лежат секреты.
# Сгенерировать новый SECRET_KEY:
#     venv\Scripts\python.exe -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# Ключ подписи сессий и токенов сброса пароля. Уникален для каждой установки.
SECRET_KEY=

# Режим отладки. На боевом сервере обязательно False.
DEBUG=True

# Хосты, с которых разрешено обслуживать запросы (через запятую).
ALLOWED_HOSTS=127.0.0.1,localhost

# ── База данных (PostgreSQL) ─────────────────────────────
# Роль и базу создать заранее (psql или pgAdmin, по одной команде за раз —
# CREATE DATABASE не выполняется внутри транзакции):
#     CREATE ROLE kursovoi_user WITH LOGIN PASSWORD 'пароль' CREATEDB;
#     CREATE DATABASE kursovoi OWNER kursovoi_user ENCODING 'UTF8';
# Право CREATEDB нужно тестам: Django создаёт временную базу test_kursovoi.
DB_NAME=kursovoi
DB_USER=kursovoi_user
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432
